
<!--<%= merchant.inspect %>-->
	<%= render(cart.line_items) %>

<br>
<div class="cart-row">
   <div class="item-price">
      <%= number_to_currency(cart.total_price, :locale => :my) %>
   </div>
      Food/Beverage total
</div>
<div class="cart-row">
   <div class="item-price">
      <%= number_to_currency(cart.delivery_fee, :locale => :my) %>
   </div>
      Delivery Fee
</div>

<div class="cart-total">
   <div class="number" id="cart-total">
      <%= number_to_currency(cart.total_order, :locale => :my) %>
   </div>
   Total
</div>

<div class="cart-checkout">
<center><%= link_to "Checkout", new_merchant_order_path(merchant), class: "btn btn-primary btn-large" %></center> <!--Trial 1-->
</div>
<center><%= button_to 'Reset Your Cart', cart, data: {confirm: 'Are you sure?'}, method: :delete, class: "btn btn-link" %></center>
